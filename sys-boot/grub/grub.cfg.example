#
# DO NOT EDIT THIS FILE
#
# It is automatically generated by grub-mkconfig using templates
# from /etc/grub.d and settings from /etc/default/grub
#

### BEGIN /etc/grub.d/00_header ###
insmod part_gpt
insmod part_msdos
if [ -s $prefix/grubenv ]; then
  load_env
fi
if [ "${next_entry}" ] ; then
   set default="${next_entry}"
   set next_entry=
   save_env next_entry
   set boot_once=true
else
   set default="0"
fi

if [ x"${feature_menuentry_id}" = xy ]; then
  menuentry_id_option="--id"
else
  menuentry_id_option=""
fi

export menuentry_id_option

if [ "${prev_saved_entry}" ]; then
  set saved_entry="${prev_saved_entry}"
  save_env saved_entry
  set prev_saved_entry=
  save_env prev_saved_entry
  set boot_once=true
fi

function savedefault {
  if [ -z "${boot_once}" ]; then
    saved_entry="${chosen}"
    save_env saved_entry
  fi
}

function load_video {
  if [ x$feature_all_video_module = xy ]; then
    insmod all_video
  else
    insmod efi_gop
    insmod efi_uga
    insmod ieee1275_fb
    insmod vbe
    insmod vga
    insmod video_bochs
    insmod video_cirrus
  fi
}

if loadfont unicode ; then
  set gfxmode=auto
  load_video
  insmod gfxterm
  set locale_dir=$prefix/locale
  set lang=en_US
  insmod gettext
fi
terminal_input console
terminal_output gfxterm
insmod part_gpt
insmod ext2
set root='hd1,gpt3'
if [ x$feature_platform_search_hint = xy ]; then
  search --no-floppy --fs-uuid --set=root --hint-bios=hd1,gpt3 --hint-efi=hd1,gpt3 --hint-baremetal=ahci1,gpt3  e3c8bb05-1e95-4114-a3ca-5efd29b83808
else
  search --no-floppy --fs-uuid --set=root e3c8bb05-1e95-4114-a3ca-5efd29b83808
fi
insmod gfxmenu
loadfont ($root)/boot/grub/themes/avouch/dejavu_mono_14.pf2
loadfont ($root)/boot/grub/themes/avouch/DejaVuSans10.pf2
loadfont ($root)/boot/grub/themes/avouch/DejaVuSans12.pf2
loadfont ($root)/boot/grub/themes/avouch/DejaVuSans-Bold14.pf2
insmod png
set theme=($root)/boot/grub/themes/avouch/theme.txt
export theme
if [ x$feature_timeout_style = xy ] ; then
  set timeout_style=menu
  set timeout=5
# Fallback normal timeout code in case the timeout_style feature is
# unavailable.
else
  set timeout=5
fi
### END /etc/grub.d/00_header ###

### BEGIN /etc/grub.d/10_linux ###
menuentry 'Avouch Linux Linux' --class avouch --class gnu-linux --class gnu --class os $menuentry_id_option 'gnulinux-simple-e3c8bb05-1e95-4114-a3ca-5efd29b83808' {
	load_video
	set gfxpayload=keep
	insmod gzio
	insmod part_gpt
	insmod ext2
	set root='hd1,gpt3'
	if [ x$feature_platform_search_hint = xy ]; then
	  search --no-floppy --fs-uuid --set=root --hint-bios=hd1,gpt3 --hint-efi=hd1,gpt3 --hint-baremetal=ahci1,gpt3  e3c8bb05-1e95-4114-a3ca-5efd29b83808
	else
	  search --no-floppy --fs-uuid --set=root e3c8bb05-1e95-4114-a3ca-5efd29b83808
	fi
	linux	/boot/vmlinuz-linux root=UUID=e3c8bb05-1e95-4114-a3ca-5efd29b83808 rw  quiet splash
	initrd	/boot/initramfs-linux.img
}
submenu 'Advanced options for Avouch Linux Linux' $menuentry_id_option 'gnulinux-advanced-e3c8bb05-1e95-4114-a3ca-5efd29b83808' {
	menuentry 'Avouch Linux Linux, with Linux linux' --class avouch --class gnu-linux --class gnu --class os $menuentry_id_option 'gnulinux-linux-advanced-e3c8bb05-1e95-4114-a3ca-5efd29b83808' {
		load_video
		set gfxpayload=keep
		insmod gzio
		insmod part_gpt
		insmod ext2
		set root='hd1,gpt3'
		if [ x$feature_platform_search_hint = xy ]; then
		  search --no-floppy --fs-uuid --set=root --hint-bios=hd1,gpt3 --hint-efi=hd1,gpt3 --hint-baremetal=ahci1,gpt3  e3c8bb05-1e95-4114-a3ca-5efd29b83808
		else
		  search --no-floppy --fs-uuid --set=root e3c8bb05-1e95-4114-a3ca-5efd29b83808
		fi
		linux	/boot/vmlinuz-linux root=UUID=e3c8bb05-1e95-4114-a3ca-5efd29b83808 rw  quiet splash
		initrd	/boot/initramfs-linux.img
	}
}

### END /etc/grub.d/10_linux ###

### BEGIN /etc/grub.d/20_linux_xen ###
### END /etc/grub.d/20_linux_xen ###

### BEGIN /etc/grub.d/30_os-prober ###
menuentry 'Windows Boot Manager (on /dev/sdb1)' --class windows --class os $menuentry_id_option 'osprober-efi-BE8A-7187' {
	insmod part_gpt
	insmod fat
	set root='hd1,gpt1'
	if [ x$feature_platform_search_hint = xy ]; then
	  search --no-floppy --fs-uuid --set=root --hint-bios=hd1,gpt1 --hint-efi=hd1,gpt1 --hint-baremetal=ahci1,gpt1  BE8A-7187
	else
	  search --no-floppy --fs-uuid --set=root BE8A-7187
	fi
	chainloader /EFI/Microsoft/Boot/bootmgfw.efi
}
### END /etc/grub.d/30_os-prober ###

### BEGIN /etc/grub.d/40_custom ###
# This file provides an easy way to add custom menu entries.  Simply type the
# menu entries you want to add after this comment.  Be careful not to change
# the 'exec tail' line above.
### END /etc/grub.d/40_custom ###

### BEGIN /etc/grub.d/41_custom ###
if [ -f  ${config_directory}/custom.cfg ]; then
  source ${config_directory}/custom.cfg
elif [ -z "${config_directory}" -a -f  $prefix/custom.cfg ]; then
  source $prefix/custom.cfg;
fi
### END /etc/grub.d/41_custom ###

### BEGIN /etc/grub.d/60_memtest86+ ###
if [ "${grub_platform}" == "pc" ]; then
    menuentry "Memory Tester (memtest86+)" --class memtest86 --class gnu --class tool {
        search --fs-uuid --no-floppy --set=root --hint-bios=hd1,gpt3 --hint-efi=hd1,gpt3 --hint-baremetal=ahci1,gpt3  e3c8bb05-1e95-4114-a3ca-5efd29b83808
        linux16 /boot/memtest86+/memtest.bin 
    }
fi
### END /etc/grub.d/60_memtest86+ ###
